{
  "name": "student-achievement-certificate-dapp",
  "version": "1.0.0",
  "description": "Blockchain final project: student achievement certificate NFT system.",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node --hostname 0.0.0.0",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "check:contract": "node scripts/check-contract.js",
    "check:latest": "node scripts/check-contract.js --latest",
    "frontend": "node server/frontend-server.js",
    "server": "node server/mock-ai-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.12.1",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.3"
  }
}
